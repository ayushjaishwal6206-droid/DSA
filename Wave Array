class Solution {
  public:
    void sortInWave(vector<int>& v) {
        int n = v.size();
        for(int i = 0; i < n; i+=2){
            if (i == n - 1){
                continue;
            }
            swap(v[i], v[i+1]);
        }
       
    }
};
